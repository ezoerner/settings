[core]
        excludesfile = ~/settings/gitignore_global
	autocrlf = input
[user]
	name = Eric Zoerner
	email = eric.zoerner@gmail.com
[credential]
	helper = osxkeychain
[filter "lfs"]
       clean = git-lfs clean -- %f
       smudge = git-lfs smudge -- %f
       process = git-lfs filter-process
       required = true

